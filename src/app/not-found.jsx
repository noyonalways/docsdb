import NotFound from "@/components/NotFound";

export const metadata = {
  title: "404 - Not Found",
};

const NotFoundPage = () => {
  return (
    <>
      <NotFound />
    </>
  );
};

export default NotFoundPage;
